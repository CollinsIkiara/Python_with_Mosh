# The following code demonstrates the use of some mathematical functions in Python. It imports the math module, defines a variable x with the value 2.9, and then prints the absolute value of -2.9, the ceiling of x, and the floor of x.

import math

x = 2.9
print(abs(-2.9))
print(math.ceil(x))
print(math.floor(x))